# Third-party notices

## React Bits SplashCursor

`src/components/SplashCursor.jsx` is adapted from the user-supplied React Bits SplashCursor JavaScript component. React Bits: https://reactbits.dev/backgrounds/splash-cursor

Local changes add desktop/reduced-motion guards, WebGL capability checks, lower simulation settings at the call site, hidden-tab work suspension, transparent overlay tuning, and cleanup of format-test resources. Consult the upstream project for its applicable licensing notices before redistributing independently of this supplied component.

## Other dependencies and assets

React, React DOM, Vite, and the Vite React plugin retain their upstream licenses, included by their installed npm packages. Skill icons load from Devicon and Simple Icons; names and logos identify their respective products. Fonts load from Google Fonts (DM Sans, Space Grotesk, JetBrains Mono).
